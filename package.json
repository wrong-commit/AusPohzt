{
    "name": "boganpost",
    "private": true,
    "workspaces": [
        "backend",
        "frontend"
    ],
    "scripts": {
        "clean": "tsc --build --clean tsconfig.build.json",
        "build": "tsc --build tsconfig.build.json",
        "watch": "tsc --build --watch"
    },
    "devDependencies": {
        "@types/cors": "^2.8.12",
        "typescript": "^4.3.5"
    }
}
